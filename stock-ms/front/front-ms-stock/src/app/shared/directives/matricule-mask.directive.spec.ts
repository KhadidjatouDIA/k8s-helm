import { MatriculeMaskDirective } from './matricule-mask.directive';

describe('MatriculeMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new MatriculeMaskDirective();
    expect(directive).toBeTruthy();
  });
});
